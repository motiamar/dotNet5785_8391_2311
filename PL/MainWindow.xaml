<Window x:Class="PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="MainWindow" Height="640" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">
    <!--External grid-->
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="pack://application:,,,/Images/Image.jpg" />
        </Grid.Background>

        <!--Header-->
        <Label Content="Login to the System" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0"/>

        <!--User Input Section-->
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <!--User ID Input-->
            <Label Content="Enter User ID:" FontSize="16" Margin="0,0,0,10"/>
            <TextBox Text="{Binding CurrentID}" Width="250" Margin="0,0,0,20"/>

            <!--Password Input-->
            <Label Content="Enter Password:" FontSize="16" Margin="0,0,0,10"/>

            <!-- Grid to contain both the PasswordBox and TextBox -->
            <Grid>
                <!-- PasswordBox -->
                <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged" Width="250" Margin="0,0,0,20" Visibility="Visible"/>

                <!-- TextBox to show password as plain text -->
                <TextBox x:Name="TextBoxPassword" Visibility="Collapsed" Width="250" Margin="0,0,0,20" IsReadOnly="True"/>
            </Grid>

            <!--CheckBox to Show/Hide Password-->
            <CheckBox x:Name="ShowPasswordCheckBox" Content="Show Password" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="0,10,0,0" Checked="ShowPasswordCheckBox_Checked" Unchecked="ShowPasswordCheckBox_Unchecked"/>

            <!--Login Button-->
            <Button Content="Login" Width="150" Background="AliceBlue" Height="40" HorizontalAlignment="Center" Click="BtnLogIn_Click"/>
        </StackPanel>
        <Label Content="בסד" HorizontalAlignment="Left" Margin="751,10,0,0" VerticalAlignment="Top" RenderTransformOrigin="-47.467,24.869"/>
    </Grid>
</Window>
